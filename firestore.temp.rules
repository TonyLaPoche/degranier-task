// RÈGLES FIRESTORE TEMPORAIRES POUR TESTS
// ⚠️ RÈGLES PERMISSIVES - À REMPLACER EN PRODUCTION

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES TEMPORAIRES - TOUT PERMIS POUR LES TESTS
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}